#include <stdio.h>
/**
 * main - print base 10 single digit numbers
 *
 * Return: Always 0(Success)
 */
int main(void)
{
	int i = 0;

	while (1 < 10)
	{
		printf("%d", i);
		printf("\n");
		i++;
	}
	return (0);
}
