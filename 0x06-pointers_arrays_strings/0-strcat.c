#include "main.h"
#include <string.h>
/**
 * *_strcat - to concatenate two strings
 * @dest: first string
 *@src: second string
 *
 *Return: one thing
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
