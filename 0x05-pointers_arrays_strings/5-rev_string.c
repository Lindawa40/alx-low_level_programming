#include <stdio.h>
#include <string.h>
/*
 * void rev_string - print string in reverse
 * @s: string to be reversed
 *
 * Return: nothing
 */
void rev_string(char *s)
{
	char s[10];
	putchar(char *s);
       strrev(*s);
putchar(char*s);
}
