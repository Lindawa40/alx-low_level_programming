#include <stdio.h>
#include "main.h"
/*
 * void print_rev - print string in reverse
 * @str: string to be reversed
 *
 * Return: nothing
 */
void print_rev(char *s)
{
	char *str;
	char *s;
	char *str == char *s;
	strrev(*s);
	int putchar(char *s);
}		
